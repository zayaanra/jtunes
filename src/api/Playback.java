package api;

public class Playback implements Runnable {
    private boolean paused;
    private boolean playing;
    
    // TODO -
    // Playback class should run the music on a background thread
    
    @Override
    public void run() {
    }
}
